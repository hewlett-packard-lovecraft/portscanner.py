from setuptools import setup

setup(
    name='portscanner.py',
    description="Python port scanner",
    version='0.0.1',
    packages=['portscannerpy'],
    install_requires=[
        'requests',
    ],
)

